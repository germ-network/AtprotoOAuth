The dev plan for this:

./LocalPackages will be spun out into their ownrepos

./Sources/TempShim (and dependencies marked as shim) are there for temporary functionality until we shuffle things into their desired place